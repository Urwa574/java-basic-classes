package class6;

public class WhileLoop6 {
    public static void main(String[] args) {
        int number=0;
        while (number<=16){
            if(number%5!=0){ //here we r trying to say if no is divided by 5 i dont want
                System.out.println(number);
            } number++;

                    int num = 1;
                    while(num<=16){ //or we can do with or||
                        if(num!=5&&num!=10&&num!=15) { // if(num==5||num==10||num==15){ or method
                            System.out.println(num);
                        }
                        num++;
                    }
                    //Write a for loop that will print out a series of numbers starting at 0 and ending at the x
            // (value must be taken from a user), exclusive.

            /*
                Scanner scan=new Scanner(System.in);
                int x=scan.nextInt();
               for(int i=0;i<x;i++)
                 System.out.print(" "+i);
             */
                }
            }
        }

